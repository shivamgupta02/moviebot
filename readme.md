# moviebot

A simple slack bot which suggest you movies based on your mood or genre or a random movie suggestion.

To use this just change the token in `moviebot.js` with the token for your slack bot.
